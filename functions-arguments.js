function math (firstArg, secondArg, thirdArg) {         //argument names are only used to reference them
  let result = secondArg * thirdArg + firstArg
  return result
}

console.log(math(53, 61, 67))
